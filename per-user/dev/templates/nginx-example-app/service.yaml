apiVersion: v1
kind: Service
metadata:
  name: nginx-example
  namespace: {{ .Values.namespacePermissions.user }}-apps
spec:
  selector:
    app: nginx-example
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
