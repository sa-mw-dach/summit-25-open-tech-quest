apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - resources/
  - operator-resources/
  - namespaces.yaml

patches:
- patch: |-
    - op: replace
      path: /spec/centralEndpoint
      value: 'central.stackrox.svc:443'
  target:
    kind: SecuredCluster